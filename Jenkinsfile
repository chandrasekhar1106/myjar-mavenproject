pipeline {
    agent any
    stages {
       stage("build"){
             step {
                echo 'It is build stage..'
             }
       }
       stage("install"){
             step {
                echo 'It is build stage..'
             }
       }
       stage("deploy") {
             step {
                echo 'It is build stage..'
             }
       }
    }
}
