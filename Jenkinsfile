pipeline {
    agent any
    stages {
       stage('build'){
             echo "It is build stage"
           }
       stage('install'){
             echo "It is install stage"
           }
       stage('deploy') {
             echo "It is deploy stage"
           }
    }
}
