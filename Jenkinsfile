pipeline {
    agent any
    stages {
       stage("build"){
             steps{
                echo 'It is build stage..'
             }
       }
       stage("install"){
             steps{
                echo 'It is build stage..'
             }
       }
       stage("deploy") {
             steps{
                echo 'It is build stage..'
             }
       }
    }
}
